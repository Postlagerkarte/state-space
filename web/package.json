{
  "name": "state-space",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A hands-on tour of graph search algorithms, built on a sliding-block puzzle",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck:scripts": "tsc -p tsconfig.scripts.json --noEmit",
    "mine": "tsx scripts/mine.ts"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@types/three": "^0.180.0",
    "puppeteer-core": "^25.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
